{
  "name": "sicp",
  "version": "1.0.0",
  "main": "sicp.js",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "jest --coverage",
    "test:watch": "jest --coverage --watchAll --notify",
    "clean": "rm -rf coverage"
  },
  "devDependencies": {
    "jest": "^26.6.3"
  }
}
